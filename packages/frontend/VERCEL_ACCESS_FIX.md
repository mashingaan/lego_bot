# 🔐 Исправление ошибки доступа Vercel к GitHub

Ошибка: **"Deployment request did not have a git author with contributing access to the project on Vercel"**

Это означает, что Vercel не имеет прав доступа к вашему GitHub репозиторию.

## 🔧 Решение 1: Переподключить GitHub к Vercel

### Шаг 1: Проверьте подключение GitHub

1. В Vercel Dashboard перейдите в **Settings** (вкладка в верхнем меню)
2. Выберите **Git** в левом меню
3. Проверьте, подключен ли GitHub аккаунт

### Шаг 2: Переподключите репозиторий

1. Если репозиторий не подключен:
   - Нажмите **"Connect Git Repository"**
   - Выберите **GitHub**
   - Авторизуйтесь через GitHub
   - Выберите репозиторий `BogdanMod/lego_bot`

2. Если репозиторий уже подключен:
   - Нажмите **"Disconnect"** рядом с репозиторием
   - Затем **"Connect Git Repository"** снова
   - Выберите `BogdanMod/lego_bot`

### Шаг 3: Проверьте права доступа

1. Убедитесь, что вы дали Vercel все необходимые права:
   - Repository access
   - Admin access (если нужно)

## 🚀 Решение 2: Использовать Vercel CLI (рекомендуется)

Если проблемы с доступом продолжаются, используйте CLI для деплоя:

### Шаг 1: Установите Vercel CLI
```bash
npm install -g vercel
```

### Шаг 2: Войдите в Vercel
```bash
vercel login
```

### Шаг 3: Перейдите в папку frontend
```bash
cd packages/frontend
```

### Шаг 4: Создайте проект (первый раз)
```bash
vercel
```

CLI спросит:
- **Set up and deploy?** → `Y`
- **Which scope?** → выберите `bogdanmod`
- **Link to existing project?** → если проект уже создан в Dashboard, выберите `Y` и укажите `lego-bot-frontend`
- **What's your project's name?** → `lego-bot-frontend`
- **In which directory is your code located?** → `./`

### Шаг 5: Задеплойте в production
```bash
vercel --prod
```

## ✅ Решение 3: Проверьте настройки GitHub App

### В GitHub:

1. Перейдите в **Settings** → **Applications** → **Installed GitHub Apps**
2. Найдите **Vercel**
3. Убедитесь, что у Vercel есть доступ к репозиторию `BogdanMod/lego_bot`

### В Vercel:

1. Перейдите в **Settings** → **Git**
2. Проверьте, что репозиторий правильно подключен
3. Если нужно, отключите и подключите заново

## 🎯 Альтернативное решение: Деплой без GitHub

Если проблемы продолжаются, можно задеплоить напрямую через CLI без GitHub:

```bash
cd packages/frontend
vercel --prod --yes
```

Это создаст деплой из локальных файлов, без привязки к GitHub.

## 📋 После успешного деплоя

1. **Скопируйте Production URL:**
   - Например: `https://lego-bot-frontend.vercel.app`

2. **Настройте автоматические деплои (если нужно):**
   - Settings → Git
   - Убедитесь, что Production Branch = `main`
   - После этого каждый push в `main` будет автоматически создавать деплой

3. **Настройте домен в @BotFather:**
   - Откройте @BotFather в Telegram
   - `/mybots` → Выберите бота → `Bot Settings` → `Domain`
   - Введите домен без `https://`: `lego-bot-frontend.vercel.app`

## 🐛 Если ничего не помогает

1. **Удалите проект в Vercel:**
   - Settings → General → Delete Project

2. **Создайте новый проект через CLI:**
   ```bash
   cd packages/frontend
   vercel --prod
   ```

3. **Или создайте проект вручную:**
   - New Project → Import Git Repository
   - Выберите `BogdanMod/lego_bot`
   - Root Directory: `packages/frontend`
   - Framework Preset: `Other`


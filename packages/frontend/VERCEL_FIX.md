# 🔧 Исправление ошибок деплоя на Vercel

Если Vercel не может задеплоить проект, выполните следующие шаги:

## 📋 Настройки в Vercel Dashboard

### 1. **Root Directory**
```
packages/frontend
```

### 2. **Framework Preset**
```
Other
```
или
```
Other (Static)
```

### 3. **Build Command**
```
echo 'No build needed'
```
или просто оставьте пустым

### 4. **Output Directory**
```
.
```
(точка - текущая директория)

### 5. **Install Command**
```
echo 'No install needed'
```
или оставьте пустым (Vercel не будет запускать npm install для статических файлов)

## 🐛 Частые ошибки и решения

### Ошибка: "Cannot find module" или "Missing dependencies"

**Решение:**
1. Убедитесь, что **Root Directory** указан как `packages/frontend`
2. Установите **Install Command** в пустое значение или `echo 'No install needed'`
3. Для статических файлов зависимости не нужны

### Ошибка: "Build failed" или "Build command failed"

**Решение:**
1. Установите **Build Command** в `echo 'No build needed'`
2. Или оставьте его пустым
3. Убедитесь, что **Framework Preset** = `Other`

### Ошибка: "Output directory not found"

**Решение:**
1. Установите **Output Directory** в `.` (точка)
2. Это означает текущую директорию (`packages/frontend`)

### Ошибка: "No matching deployment found"

**Решение:**
1. Проверьте, что файлы находятся в `packages/frontend/`
2. Убедитесь, что `index.html` существует в этой папке
3. Проверьте, что **Root Directory** правильный

## ✅ Правильная конфигурация для статического сайта

```
Vercel Team: bogdanmod's projects (Hobby)
Project Name: lego-bot-frontend
Root Directory: packages/frontend
Framework Preset: Other
Build Command: (пусто) или echo 'No build needed'
Output Directory: .
Install Command: (пусто) или echo 'No install needed'
```

## 📁 Структура файлов

Убедитесь, что в `packages/frontend/` находятся:
- ✅ `index.html`
- ✅ `style.css`
- ✅ `script.js`
- ✅ `vercel.json` (опционально, для настройки)

## 🔄 Пересоздание проекта

Если ничего не помогает:

1. **Удалите проект в Vercel**
2. **Создайте новый проект**
3. **Используйте правильные настройки** (см. выше)
4. **Деплой должен пройти успешно**

## 📝 Альтернативный способ (через CLI)

Если Dashboard не работает, используйте Vercel CLI:

```bash
cd packages/frontend
vercel --prod
```

Vercel CLI автоматически определит настройки для статического сайта.


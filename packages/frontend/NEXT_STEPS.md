# ✅ Frontend успешно задеплоен!

**URL:** `https://lego-bot-frontend.vercel.app`

## 📋 Что делать дальше

### 1. 🔐 Настроить домен в @BotFather

Для работы Telegram Login Widget нужно добавить домен в настройки бота:

1. **Откройте @BotFather в Telegram**
2. Отправьте команду `/mybots`
3. Выберите вашего бота (`@legobusiness1_bot`)
4. Нажмите **"Bot Settings"** (Настройки бота)
5. Выберите **"Domain"** (Домен)
6. Введите домен без `https://`:
   ```
   lego-bot-frontend.vercel.app
   ```
7. Нажмите **"Set"** (Установить)

⚠️ **Важно:** Вводите только домен, без `https://` и без `/` в конце!

### 2. 🔧 Обновить FRONTEND_URL в основном боте

Нужно обновить переменную окружения в основном боте для правильной работы CORS:

1. Откройте файл `.env` в корне проекта
2. Найдите строку `FRONTEND_URL`
3. Обновите на:
   ```
   FRONTEND_URL=https://lego-bot-frontend.vercel.app
   ```
4. Если строки нет, добавьте её

### 3. 🔧 Обновить API_BASE_URL в frontend (если нужно)

Если ваш API сервер находится по другому адресу, обновите `script.js`:

1. Откройте `packages/frontend/script.js`
2. Найдите строку:
   ```javascript
   const API_BASE_URL = window.location.hostname === 'localhost' 
     ? 'http://localhost:3000' 
     : 'https://your-api-domain.com';
   ```
3. Замените `'https://your-api-domain.com'` на адрес вашего API сервера

### 4. 🚀 Перезапустить основной бот

После обновления `.env`:

```bash
cd packages/core
npm run dev
```

Или если используете Docker:

```bash
docker-compose restart
```

## ✅ Проверка работы

### 1. Откройте frontend в браузере

Перейдите по адресу: https://lego-bot-frontend.vercel.app

### 2. Проверьте Telegram Login Widget

- Должна появиться кнопка **"Login with Telegram"**
- При нажатии должно открыться окно авторизации Telegram
- После авторизации должен появиться список ваших ботов

### 3. Протестируйте редактирование схемы

1. Выберите бота из списка
2. Нажмите **"Загрузить схему"**
3. Отредактируйте JSON-схему
4. Нажмите **"Сохранить схему"**
5. Проверьте, что схема сохранилась

## 🐛 Устранение проблем

### Проблема: Telegram Login Widget не появляется

**Решение:**
1. Проверьте, что домен добавлен в @BotFather
2. Убедитесь, что в `index.html` указан правильный username бота (без `@`)
3. Очистите кеш браузера (Ctrl+Shift+R или Cmd+Shift+R)

### Проблема: "CORS error" при запросах к API

**Решение:**
1. Убедитесь, что `FRONTEND_URL` в `.env` установлен правильно
2. Перезапустите основной бот
3. Проверьте, что CORS настроен в `packages/core/src/index.ts`

### Проблема: "Unauthorized" при запросах к API

**Решение:**
1. Убедитесь, что вы авторизовались через Telegram Login Widget
2. Проверьте, что `TELEGRAM_BOT_TOKEN` установлен в `.env` основного бота
3. Проверьте логи API сервера

## 📚 Следующие шаги для продакшена

1. **Настройте кастомный домен** (опционально):
   - Vercel Dashboard → Settings → Domains
   - Добавьте ваш домен

2. **Настройте автоматические деплои**:
   - Settings → Git
   - Убедитесь, что Production Branch = `main`
   - Теперь каждый push в `main` будет автоматически создавать деплой

3. **Добавьте переменные окружения** (если нужно):
   - Vercel Dashboard → Settings → Environment Variables
   - Добавьте `API_BASE_URL` если нужно

## 🎉 Готово!

Теперь у вас есть рабочий веб-интерфейс для редактирования схем ботов!

- ✅ Frontend: https://lego-bot-frontend.vercel.app
- ✅ API: ваш API сервер
- ✅ Telegram Bot: ваш основной бот

Можно начинать использовать интерфейс для редактирования схем!


# 📧 Как добавить email bogdan.rudenko05@mail.ru в Vercel

## 🎯 Способ 1: Добавить email в профиль Vercel (рекомендуется)

### Шаг 1: Откройте настройки профиля

1. Войдите в Vercel Dashboard: https://vercel.com
2. Нажмите на ваш **аватар** (правый верхний угол)
3. Выберите **"Settings"** или **"Account Settings"**

### Шаг 2: Добавьте email

1. Перейдите в раздел **"Email"** или **"Account"**
2. Найдите секцию **"Email Addresses"** или **"Additional Emails"**
3. Нажмите **"Add Email"** или **"Add Another Email"**
4. Введите: `bogdan.rudenko05@mail.ru`
5. Подтвердите email (проверьте почту и перейдите по ссылке)

### Шаг 3: Проверьте email

1. Проверьте почту `bogdan.rudenko05@mail.ru`
2. Найдите письмо от Vercel с подтверждением
3. Перейдите по ссылке для подтверждения

## 🎯 Способ 2: Добавить email через команду (если используете Team)

### Шаг 1: Откройте настройки команды

1. В Vercel Dashboard перейдите в **"Settings"**
2. Выберите **"Members"** или **"Team"** → **"Members"**

### Шаг 2: Пригласите по email

1. Нажмите **"Invite Member"** или **"Add Member"**
2. Введите email: `bogdan.rudenko05@mail.ru`
3. Выберите роль (обычно **"Member"** или **"Developer"**)
4. Отправьте приглашение

### Шаг 3: Примите приглашение

1. Проверьте почту `bogdan.rudenko05@mail.ru`
2. Найдите приглашение от Vercel
3. Перейдите по ссылке и примите приглашение

## 🎯 Способ 3: Изменить Git email (альтернатива)

Если не хотите добавлять email в Vercel, можно изменить Git email на тот, который уже есть в Vercel:

### Шаг 1: Узнайте email в Vercel

1. Vercel Dashboard → Settings → Profile
2. Посмотрите, какой email указан

### Шаг 2: Измените Git email

```bash
cd /Users/bogdan.rudenko/Desktop/lego_bot
git config user.email "ваш-vercel-email@example.com"
git config user.name "BogdanMod"
```

### Шаг 3: Обновите последний коммит

```bash
git commit --amend --reset-author --no-edit
git push --force-with-lease origin main
```

## ✅ После добавления email

1. **Подождите несколько минут** (Vercel может обновить доступ)
2. **Запустите деплой снова:**
   ```bash
   cd packages/frontend
   vercel --prod
   ```

## 🔍 Проверка

После добавления email проверьте:

1. **В Vercel Dashboard:**
   - Settings → Profile → Email Addresses
   - Должен быть виден `bogdan.rudenko05@mail.ru`

2. **В Git:**
   ```bash
   git config user.email
   ```
   Должен показать `bogdan.rudenko05@mail.ru`

3. **Запустите деплой:**
   ```bash
   cd packages/frontend
   vercel --prod
   ```

## 🐛 Если не получается добавить email

### Вариант 1: Использовать существующий email Vercel

1. Узнайте email в Vercel (Settings → Profile)
2. Измените Git email на этот:
   ```bash
   git config user.email "ваш-vercel-email@example.com"
   git commit --amend --reset-author --no-edit
   git push --force-with-lease origin main
   ```

### Вариант 2: Деплой без проверки Git

```bash
cd packages/frontend
vercel --prod --yes
```

Но это может не сработать, если Vercel все равно проверяет Git автора.

## 📝 Краткая инструкция

**Самый простой способ:**

1. Vercel Dashboard → **Settings** → **Profile**
2. Найдите **"Email Addresses"** или **"Add Email"**
3. Добавьте `bogdan.rudenko05@mail.ru`
4. Подтвердите через почту
5. Запустите `cd packages/frontend && vercel --prod`


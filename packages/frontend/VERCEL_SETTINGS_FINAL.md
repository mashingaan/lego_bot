# ✅ Правильные настройки Vercel для статического frontend

## 🔧 Настройки Build and Deployment

### 1. Root Directory
- **Значение:** оставьте **пустым** (не указывайте `.`)
- **Почему:** Vercel не принимает `.` в Root Directory (см. предупреждение)
- **Так как:** Вы запускаете CLI из папки `packages/frontend`, Vercel автоматически использует текущую директорию

**Действия:**
1. В поле **Root Directory** удалите `.` (точку)
2. Оставьте поле **пустым**
3. Нажмите **Save**

### 2. Output Directory
- **Значение:** `.` (точка) - это правильно
- **Override:** Включен (синий) - правильно
- **Не меняйте** - это корректная настройка

### 3. Install Command
- **Текущее:** Не переопределен (будет использовать npm/yarn/pnpm)
- **Рекомендуется:** Переопределить на пустую команду для статического сайта

**Действия:**
1. Включите **Override** для Install Command
2. В поле введите: `true` (или оставьте пустым)
3. Нажмите **Save**

### 4. Build Command (если есть в настройках)
- **Значение:** оставьте **пустым** или `true`
- **Override:** Включен
- Для статического сайта build не нужен

### 5. Development Command
- **Значение:** `None` - это правильно
- **Не меняйте** - для статического сайта это не используется

## 📋 Итоговые правильные настройки

### Build and Deployment:
- ✅ **Root Directory:** (пусто) - не указывайте `.`
- ✅ **Output Directory:** `.` (Override включен)
- ✅ **Install Command:** `true` (Override включен) или пусто
- ✅ **Build Command:** (пусто) или `true` (Override включен)
- ✅ **Development Command:** `None`

### General:
- ✅ **Framework Preset:** `Other` или `Other (Static)`

## 🔄 После изменения настроек

1. **Сохраните настройки** (нажмите Save в каждой секции)
2. **Запустите деплой:**
   ```bash
   cd packages/frontend
   vercel --prod
   ```
3. **Или создайте новый деплой** через Dashboard:
   - Deployments → Create Deployment
   - Branch: `main`
   - Нажмите Deploy

## ⚠️ Важно

**Root Directory должен быть ПУСТЫМ**, а не `.` (точка)!

Vercel показывает предупреждение:
> "If defined, the Root Directory must be a relative path not starting with './' and not including '../' or other special characters."

Точка `.` может интерпретироваться как `./`, что не разрешено. Оставьте поле пустым.

## ✅ После правильной настройки

1. **Root Directory:** пустое поле
2. **Output Directory:** `.` (Override включен)
3. **Install Command:** `true` или пусто (Override включен)
4. Сохраните все настройки
5. Запустите `vercel --prod`

Деплой должен пройти успешно!


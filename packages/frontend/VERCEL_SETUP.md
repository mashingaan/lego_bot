# 🚀 Настройка развертывания на Vercel

Инструкция по настройке параметров при создании проекта в Vercel.

## 📋 Параметры в форме Vercel "New Project"

### 1. **Vercel Team**
- **Выберите:** `bogdanmod's projects` (ваш аккаунт)
- **План:** `Hobby` (бесплатный план)

### 2. **Project Name**
- **Введите:** `lego-bot-frontend` (или любое другое имя по вашему выбору)
- ⚠️ **Важно:** Это имя будет использоваться в URL: `https://lego-bot-frontend.vercel.app`

### 3. **Framework Preset**
- **Выберите:** `Other` или `Other (Static)`
- ⚠️ **НЕ выбирайте:** `ex Express` (это для backend)
- Frontend - это статические файлы (HTML/CSS/JS)

### 4. **Root Directory**
- **Введите:** `packages/frontend`
- Или нажмите кнопку **"Edit"** и укажите путь: `packages/frontend`
- ⚠️ **Важно:** Vercel должен знать, что нужно деплоить именно папку `frontend`, а не корень репозитория

### 5. **Build and Output Settings** (раскройте, если нужно)

Для статического сайта можно оставить по умолчанию:
- **Build Command:** можно оставить пустым или `echo "No build needed"`
- **Output Directory:** `.` (текущая директория, т.е. `packages/frontend`)
- **Install Command:** можно оставить пустым или `echo "No install needed"`

### 6. **Environment Variables** (раскройте, если нужно)

Здесь можно добавить переменные окружения для frontend:

- `API_BASE_URL` = `https://your-api-domain.com` (URL вашего API сервера)

⚠️ **Примечание:** В текущем коде `script.js` API URL определяется автоматически, но если нужно переопределить, добавьте переменную.

## 🔍 Пошаговая инструкция

1. **На странице создания проекта в Vercel:**

   - **Repository:** `BogdanMod/lego_bot` (уже выбрано)
   - **Branch:** `main` (уже выбрано)
   - **Root Directory:** нажмите **"Edit"** и введите `packages/frontend`

2. **Project Name:**
   ```
   lego-bot-frontend
   ```

3. **Framework Preset:**
   ```
   Other
   ```
   или
   ```
   Other (Static)
   ```

4. **Vercel Team:**
   - Выберите `bogdanmod's projects`
   - План: `Hobby`

5. **Нажмите "Deploy"**

## ⚙️ После деплоя

После успешного деплоя:

1. **Скопируйте URL проекта:**
   - Например: `https://lego-bot-frontend.vercel.app`

2. **Настройте домен в @BotFather:**
   - Откройте @BotFather
   - `/mybots` → Выберите бота → `Bot Settings` → `Domain`
   - Введите домен без `https://`: `lego-bot-frontend.vercel.app`

3. **Обновите `script.js` (если нужно):**
   - Если API URL отличается от `http://localhost:3000`, обновите `API_BASE_URL`:
   ```javascript
   const API_BASE_URL = 'https://your-api-domain.com';
   ```

4. **Настройте CORS в API сервере:**
   - В `.env` основного бота добавьте:
   ```
   FRONTEND_URL=https://lego-bot-frontend.vercel.app
   ```

## 🔧 Альтернативная настройка через vercel.json

Если нужно более точно настроить поведение, используйте `vercel.json` (уже создан в `packages/frontend/`):

```json
{
  "version": 2,
  "builds": [
    {
      "src": "index.html",
      "use": "@vercel/static"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/$1"
    }
  ]
}
```

## 🐛 Возможные проблемы

### Проблема: "Cannot find module"

**Решение:** Убедитесь, что Root Directory указан как `packages/frontend`, а не корень репозитория.

### Проблема: "404 Not Found"

**Решение:** 
- Проверьте, что `index.html` находится в `packages/frontend/`
- Убедитесь, что Root Directory правильный

### Проблема: Telegram Login Widget не работает

**Решение:**
- Проверьте, что домен добавлен в @BotFather
- Убедитесь, что домен в настройках совпадает с Vercel URL (без `https://`)

## 📝 Краткая памятка

```
✅ Vercel Team: bogdanmod's projects (Hobby)
✅ Project Name: lego-bot-frontend
✅ Framework Preset: Other (или Other (Static))
✅ Root Directory: packages/frontend
✅ Build Command: (пусто)
✅ Output Directory: .
✅ Install Command: (пусто)
```

## 🔗 Полезные ссылки

После деплоя вы получите:
- **Production URL:** `https://lego-bot-frontend.vercel.app`
- **Preview URLs:** Для каждого коммита создается preview URL

Используйте Production URL в настройках @BotFather.


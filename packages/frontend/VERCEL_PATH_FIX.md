# 🔧 Исправление ошибки пути Vercel

Ошибка: **"The provided path 'packages/frontend/packages/frontend' does not exist"**

## 🔍 Проблема

Vercel пытается использовать путь `packages/frontend/packages/frontend` - то есть он дублирует `packages/frontend`. Это происходит потому, что:

1. В настройках проекта Vercel уже указан **Root Directory** = `packages/frontend`
2. Вы запускаете CLI из папки `packages/frontend`
3. Vercel добавляет Root Directory к текущему пути → получается `packages/frontend/packages/frontend`

## ✅ Решение

Нужно изменить **Root Directory** в настройках Vercel на `.` (точка) или оставить пустым.

### Шаг 1: Откройте настройки проекта в Vercel

1. Перейдите в Vercel Dashboard
2. Откройте проект `lego-bot-frontend`
3. Перейдите во вкладку **Settings**
4. Выберите **General** в левом меню

### Шаг 2: Измените Root Directory

1. Найдите поле **"Root Directory"**
2. Измените значение с `packages/frontend` на `.` (точка)
3. Или оставьте его **пустым**
4. Нажмите **Save**

### Шаг 3: Задеплойте снова

После изменения настроек:

```bash
cd packages/frontend
vercel --prod
```

Теперь Vercel будет использовать текущую директорию (`.`) и не будет дублировать путь.

## 🎯 Альтернативное решение: Использовать корень проекта

Если хотите использовать Root Directory = `packages/frontend`, запускайте CLI из корня проекта:

```bash
cd ~/Desktop/lego_bot
vercel --prod --cwd packages/frontend
```

Но проще изменить Root Directory на `.` в настройках.

## 📋 Правильные настройки для Vercel

После исправления настройки должны быть:

- **Root Directory:** `.` (точка) или пусто
- **Framework Preset:** `Other` или `Other (Static)`
- **Build Command:** (пусто) или `true`
- **Output Directory:** `.`
- **Install Command:** (пусто)

## ✅ После исправления

1. Сохраните настройки в Vercel Dashboard
2. Запустите снова:
   ```bash
   cd packages/frontend
   vercel --prod
   ```
3. Деплой должен пройти успешно!


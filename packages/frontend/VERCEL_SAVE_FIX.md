# 💾 Решение проблемы сохранения настроек в Vercel

Проблема: Vercel не дает сохранить изменения, если поля пустые.

## ✅ Решение 1: Изменить значения, чтобы Vercel увидел изменение

### Шаг 1: Build Command

1. Включите **Override** для Build Command
2. Введите временное значение: `echo "No build needed"`
3. Нажмите **Save**
4. Вернитесь в настройки
5. Удалите текст, оставьте поле **пустым**
6. Нажмите **Save** снова

### Шаг 2: Install Command

1. Включите **Override** для Install Command
2. Введите временное значение: `echo "No install needed"`
3. Нажмите **Save**
4. Вернитесь в настройки
5. Удалите текст, оставьте поле **пустым**
6. Нажмите **Save** снова

## ✅ Решение 2: Использовать vercel.json (уже настроен)

`vercel.json` уже содержит правильные настройки:
```json
{
  "buildCommand": "",
  "installCommand": "",
  "outputDirectory": ".",
  "framework": null
}
```

Но если Vercel не видит эти настройки, попробуйте:

1. Удалите `vercel.json` из проекта
2. Настройте все через Dashboard
3. Или наоборот - удалите настройки из Dashboard и используйте только `vercel.json`

## ✅ Решение 3: Создать проект заново (если ничего не помогает)

Если ничего не помогает, можно пересоздать проект:

### Шаг 1: Удалите текущий проект

1. Vercel Dashboard → Settings → General
2. Прокрутите вниз
3. Нажмите **"Delete Project"**

### Шаг 2: Создайте новый проект

1. Vercel Dashboard → **"Add New Project"**
2. Выберите репозиторий `BogdanMod/lego_bot`
3. В настройках проекта:
   - **Root Directory:** `packages/frontend`
   - **Framework Preset:** `Other` или `Other (Static)`
   - **Build Command:** оставьте пустым или `true`
   - **Output Directory:** `.`
   - **Install Command:** оставьте пустым или `true`
4. Нажмите **"Deploy"**

### Шаг 3: После успешного деплоя

1. Проверьте, что деплой успешен
2. Обновите настройки, если нужно
3. Теперь Save должен работать

## ✅ Решение 4: Использовать CLI для настройки

Можно настроить проект через CLI, а не через Dashboard:

```bash
cd packages/frontend
vercel --prod
```

При первом запуске CLI спросит настройки, и вы сможете указать правильные значения.

## 🎯 Рекомендация

**Попробуйте Решение 1** - изменить значения сначала на временные, сохранить, потом очистить. Это заставит Vercel увидеть изменения.

Если не помогает, попробуйте **Решение 3** - пересоздать проект с правильными настройками с самого начала.


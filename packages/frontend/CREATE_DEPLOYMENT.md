# 🚀 Создание деплоя вручную на Vercel

Если в Deployments показывает "No Results", создайте деплой вручную.

## 📋 Способ 1: Через Vercel Dashboard

### Шаг 1: Очистите фильтры
1. Нажмите **"Clear Filters"** внизу страницы Deployments
2. Это покажет все деплои, если они есть

### Шаг 2: Создайте новый деплой
1. Нажмите кнопку **"Create Deployment"** (обычно справа вверху)
2. Выберите:
   - **Branch:** `main`
   - **Framework Preset:** `Other` или `Other (Static)`
   - **Root Directory:** `packages/frontend`
3. Нажмите **"Deploy"**

## 💻 Способ 2: Через Vercel CLI

### Шаг 1: Установите Vercel CLI (если еще не установлен)
```bash
npm install -g vercel
```

### Шаг 2: Войдите в Vercel
```bash
vercel login
```

### Шаг 3: Перейдите в папку frontend
```bash
cd packages/frontend
```

### Шаг 4: Создайте production деплой
```bash
vercel --prod
```

CLI спросит:
- **Set up and deploy?** → `Y`
- **Which scope?** → выберите ваш аккаунт
- **Link to existing project?** → `N` (или `Y` если проект уже создан)
- **What's your project's name?** → `lego-bot-frontend`
- **In which directory is your code located?** → `./`

После этого деплой создастся автоматически!

## ⚙️ Проверьте настройки проекта

Если деплои не создаются автоматически при пуше в GitHub:

### Settings → Git
- **Production Branch:** `main`
- **Root Directory:** `packages/frontend` (если нужно)

### Settings → General
- **Framework Preset:** `Other`
- **Root Directory:** `packages/frontend`
- **Build Command:** (пусто)
- **Output Directory:** `.`
- **Install Command:** (пусто)

## ✅ После создания деплоя

1. **Подождите завершения деплоя** (обычно 1-2 минуты)
2. **Промотируйте деплой в Production:**
   - Откройте созданный деплой
   - Нажмите на три точки (⋯)
   - Выберите **"Promote to Production"**
3. **Скопируйте Production URL:**
   - Например: `https://lego-bot-frontend.vercel.app`
4. **Настройте домен в @BotFather:**
   - Откройте @BotFather
   - `/mybots` → Выберите бота → `Bot Settings` → `Domain`
   - Введите домен без `https://`: `lego-bot-frontend.vercel.app`

## 🐛 Если деплой падает

Проверьте логи:
1. Откройте деплой
2. Нажмите **"Build Logs"**
3. Найдите ошибку и исправьте

Типичные проблемы:
- **"Root Directory not found"** → Проверьте Root Directory в Settings
- **"Build command failed"** → Убедитесь, что Build Command пустой или `true`
- **"No index.html"** → Проверьте, что файлы находятся в `packages/frontend/`


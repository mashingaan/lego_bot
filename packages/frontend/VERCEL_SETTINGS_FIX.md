# 🔧 Исправление настроек Vercel для статического сайта

## ⚠️ Проблема

На скриншоте видно, что:
- ✅ Framework Preset: `Other` (правильно)
- ❌ Build Command: **Override выключен** - Vercel пытается запустить `npm run build`
- ❌ Install Command: **Override выключен** - Vercel пытается установить зависимости

## ✅ Решение

### Шаг 1: Build Command

1. Найдите поле **"Build Command"**
2. **Включите переключатель "Override"** (должен стать синим)
3. **Удалите весь текст** из поля (должно быть пусто)
4. Оставьте поле **полностью пустым**

### Шаг 2: Install Command

1. Найдите поле **"Install Command"**
2. **Включите переключатель "Override"** (должен стать синим)
3. **Удалите весь текст** из поля (должно быть пусто)
4. Оставьте поле **полностью пустым**

### Шаг 3: Сохраните настройки

1. Нажмите кнопку **"Save"** внизу страницы
2. Дождитесь подтверждения сохранения

### Шаг 4: Создайте новый деплой

1. Перейдите во вкладку **"Deployments"**
2. Нажмите **"Create Deployment"**
3. Выберите:
   - **Branch:** `main`
   - Нажмите **"Deploy"**

## 📋 Итоговые правильные настройки

После исправления должно быть:

- ✅ **Framework Preset:** `Other`
- ✅ **Build Command:** (пусто) + **Override включен** (синий)
- ✅ **Output Directory:** `.` + **Override включен** (синий)
- ✅ **Install Command:** (пусто) + **Override включен** (синий)
- ✅ **Development Command:** `None` (можно не трогать)

## 🎯 Почему это важно

Если **Override выключен** для Build Command и Install Command:
- Vercel будет использовать значения по умолчанию
- Vercel попытается запустить `npm run build`
- Vercel попытается установить зависимости
- Это вызовет ошибки, так как frontend - статический сайт

Если **Override включен** и поля пустые:
- Vercel не будет запускать сборку
- Vercel не будет устанавливать зависимости
- Vercel просто скопирует статические файлы
- Деплой пройдет успешно

## ✅ После исправления

1. Сохраните настройки
2. Создайте новый деплой
3. Дождитесь завершения (обычно 10-20 секунд для статического сайта)
4. Проверьте, что деплой успешен (зеленый статус)

## 🐛 Если все еще падает

Если после исправления деплой все еще падает:

1. Проверьте логи: Deployments → последний деплой → Build Logs
2. Убедитесь, что все Override включены
3. Убедитесь, что Build Command и Install Command полностью пустые
4. Попробуйте удалить проект и создать заново (крайний случай)


# 🚀 Решение проблемы деплоя статического frontend

Проблема: Vercel пытается собрать проект через Turbo, хотя frontend - это статический сайт.

## ✅ Окончательное решение

### Вариант 1: Использовать отдельный проект Vercel без Turbo

**Проблема:** Vercel видит `turbo.json` в корне репозитория и пытается собрать весь проект.

**Решение:** Убедитесь, что настройки в Vercel Dashboard правильные:

#### Settings → General:
- **Framework Preset:** `Other` или `Other (Static)` ⚠️ **ОБЯЗАТЕЛЬНО!**
- **Root Directory:** (пусто) - НЕ указывайте `packages/frontend`

#### Settings → Build and Deployment:
- **Build Command:** (полностью пустое поле) ⚠️ **ОБЯЗАТЕЛЬНО!**
- **Install Command:** (полностью пустое поле) ⚠️ **ОБЯЗАТЕЛЬНО!**
- **Output Directory:** `.`

#### Settings → Git:
- **Production Branch:** `main`
- **Root Directory:** (пусто) или `packages/frontend` (но не `.`)

### Вариант 2: Исключить Turbo для frontend

Создайте файл `packages/frontend/.vercelignore` (уже создан) и убедитесь, что он содержит:
```
turbo.json
package.json
package-lock.json
```

Но это может не помочь, если Vercel все равно видит turbo.json в корне.

### Вариант 3: Упростить vercel.json (уже сделано)

`vercel.json` уже упрощен до минимума:
```json
{
  "buildCommand": "",
  "installCommand": "",
  "outputDirectory": ".",
  "framework": null
}
```

### Вариант 4: Переместить frontend в отдельный репозиторий (крайний случай)

Если ничего не помогает, можно создать отдельный репозиторий только для frontend, но это крайний случай.

## 🎯 Проверка настроек

### 1. Откройте Vercel Dashboard → Settings → General

Проверьте:
- ✅ Framework Preset: **Other (Static)** или **Other**
- ✅ Root Directory: (пусто) - **ОЧЕНЬ ВАЖНО!**

### 2. Откройте Settings → Build and Deployment

Проверьте:
- ✅ Build Command: (полностью пустое поле)
- ✅ Install Command: (полностью пустое поле)
- ✅ Output Directory: `.`

### 3. Убедитесь, что файлы на месте

В `packages/frontend/` должны быть:
- ✅ `index.html`
- ✅ `style.css`
- ✅ `script.js`
- ✅ `vercel.json`

## 🔄 После изменения настроек

1. **Сохраните все настройки** в Vercel Dashboard
2. **Удалите последний неуспешный деплой** (если нужно)
3. **Создайте новый деплой:**
   - Deployments → Create Deployment
   - Branch: `main`
   - Нажмите Deploy

Или Vercel автоматически передеплоит после пуша в `main`.

## 🐛 Если деплой все равно падает

### Проблема: "Turbo detected" в логах

**Решение:**
1. Убедитесь, что Framework Preset = `Other (Static)`
2. Убедитесь, что Build Command = (пусто)
3. Создайте новый деплой (не Redeploy)

### Проблема: "npm install" все равно запускается

**Решение:**
1. Убедитесь, что Install Command = (пусто)
2. Убедитесь, что package.json максимально упрощен (уже сделано)
3. Удалите package.json из frontend (но это может вызвать проблемы)

### Проблема: "Cannot find module" или другие ошибки сборки

**Решение:**
1. Это означает, что Vercel все равно пытается собрать проект
2. Убедитесь, что Framework Preset = `Other (Static)`
3. Убедитесь, что все команды пустые

## ✅ Идеальная конфигурация

```
Framework Preset: Other (Static) ⚠️ КРИТИЧЕСКИ ВАЖНО!
Root Directory: (пусто)
Build Command: (пусто)
Install Command: (пусто)
Output Directory: .
Production Branch: main
```

Если все это настроено, но деплой все равно падает - пришлите логи последнего деплоя.


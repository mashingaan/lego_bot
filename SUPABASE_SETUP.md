# 🗄️ Настройка PostgreSQL в Supabase

## 📋 Рекомендуемые настройки при создании проекта

### Основные поля (уже заполнены):

1. **Organization:** `BogdanMod's Org` ✅
2. **Project name:** `lego-bot-api` ✅
3. **Database password:** `xiKfa1-cetsus-negqys` ✅ (уже сгенерирован, надежный)
4. **Region:** `Europe` ✅ (или ближайший к вашему Vercel серверу)

### Security Options (Секретные настройки)

**Рекомендуется:** Оставить по умолчанию (не менять)

Обычно в Security Options есть:
- **IP Restrictions** - можно оставить пустым (разрешить доступ отовсюду)
- **SSL Mode** - обычно уже включен по умолчанию
- **Connection Pooling** - можно оставить по умолчанию

**Для вашего проекта:**
- Не нужно ограничивать IP (Vercel использует разные IP)
- SSL уже включен по умолчанию
- Connection Pooling можно оставить как есть

### Advanced Configuration (Расширенные настройки)

**Рекомендуется:** Оставить по умолчанию (не менять)

Обычно в Advanced Configuration есть:
- **Database Version** - оставить последнюю версию (PostgreSQL 15 или 16)
- **Compute Resources** - для Free Tier оставить минимальные
- **Backup Settings** - можно оставить по умолчанию

**Для вашего проекта:**
- Версия PostgreSQL по умолчанию подойдет
- Free Tier имеет ограниченные ресурсы, но этого достаточно
- Бэкапы можно настроить позже, если нужно

## ✅ Итоговые рекомендации

### Что оставить как есть:

1. **Security Options:**
   - Не менять (оставить по умолчанию)
   - IP Restrictions - пусто (разрешить все)
   - SSL - включен по умолчанию

2. **Advanced Configuration:**
   - Не менять (оставить по умолчанию)
   - Database Version - последняя (PostgreSQL 15/16)
   - Compute - минимальные для Free Tier

### Что проверить:

1. **Region:** Убедитесь, что выбран ближайший регион к Vercel
   - Если Vercel использует США - выберите "United States"
   - Если Vercel использует Европу - выберите "Europe" ✅ (уже выбрано)

2. **Database Password:** 
   - Уже сгенерирован надежный пароль ✅
   - Обязательно скопируйте его! (он больше не будет показан)

## 📝 Следующие шаги

1. **Нажмите "Create new project"**
2. **Дождитесь создания проекта** (обычно 1-2 минуты)
3. **После создания:**
   - Перейдите в Settings → Database
   - Найдите "Connection string" или "Connection pooling"
   - Скопируйте Connection string (URI)
   - Он будет выглядеть примерно так:
     ```
     postgresql://postgres:xiKfa1-cetsus-negqys@db.xxxxx.supabase.co:5432/postgres
     ```

4. **Обновите в Vercel:**
   - Settings → Environment Variables
   - Найдите `DATABASE_URL`
   - Замените значение на скопированный Connection string
   - Сохраните

5. **Пересоберите проект в Vercel** (Redeploy)

## ⚠️ Важно

- **Скопируйте пароль БД!** Он больше не будет показан после создания
- **Connection string** можно найти в Settings → Database после создания проекта
- Для Free Tier все настройки по умолчанию подойдут

## 🔍 После создания проекта

Проверьте подключение:
```
https://lego-bot-core.vercel.app/health
```

Должен вернуть:
```json
{
  "status": "ok",
  "databases": {
    "postgres": "connected"
  }
}
```


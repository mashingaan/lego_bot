# 🔧 Исправление предупреждения crypto при деплое на Vercel

## Проблема

При деплое на Vercel появляется предупреждение:
```
npm warn deprecated crypto@1.0.1: This package is no longer supported. 
It's now a built-in Node module. If you've depended on crypto, you should switch to the one that's built-in.
```

## ✅ Решение

Это **предупреждение, а не ошибка**. Деплой должен пройти успешно. Но для чистоты кода были внесены следующие изменения:

### 1. Исправлен импорт crypto в `packages/core/src/index.ts`

**Было:**
```typescript
import crypto from 'crypto';
```

**Стало:**
```typescript
import * as crypto from 'crypto';
```

Это правильный способ импортировать встроенный модуль Node.js в TypeScript.

### 2. Создан `.npmrc` файл

Добавлен `.npmrc` в корень проекта для настройки npm (опционально).

### 3. Проверка использования crypto

В коде правильно используется встроенный модуль `crypto` из Node.js:
- `packages/core/src/index.ts` - для проверки Telegram auth
- `packages/core/src/utils/encryption.ts` - для шифрования токенов
- `packages/router/src/utils/encryption.ts` - для расшифровки токенов

## 📝 Следующие шаги

1. **Закоммитьте изменения:**
   ```bash
   git add packages/core/src/index.ts .npmrc package.json
   git commit -m "Fix: Use built-in crypto module instead of deprecated package"
   git push
   ```

2. **Vercel автоматически пересоберет проект**

3. **Если предупреждение все еще появляется:**
   - Это нормально - это предупреждение от одной из зависимостей
   - Деплой должен пройти успешно
   - Предупреждение не влияет на работу приложения

## 🔍 Дополнительная информация

Предупреждение может исходить от одной из зависимостей проекта (например, старая версия какой-то библиотеки). Это не критично, так как:
- Мы используем встроенный модуль Node.js `crypto`
- Не устанавливаем пакет `crypto` напрямую
- Все функции криптографии работают через встроенный модуль

## ⚠️ Важно

- Это **предупреждение**, а не ошибка
- Деплой должен пройти успешно
- Если деплой падает, это не из-за этого предупреждения - проверьте другие ошибки в логах


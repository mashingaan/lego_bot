# 🔧 Исправление: Vercel отдает исходный код вместо выполнения

## ❌ Проблема

При открытии `https://lego-bot-core.vercel.app/health` отображается исходный JavaScript код вместо JSON ответа.

**Причина:** Vercel настроен как статический сайт, а не как Node.js приложение.

## ✅ Решение

### Шаг 1: Проверить настройки Vercel

1. В Vercel откройте проект `lego-bot-core`
2. Перейдите в **Settings → General**
3. Проверьте:
   - **Framework Preset:** должно быть `Other` или `Other (Node.js)`
   - **Root Directory:** `packages/core`
   - **Output Directory:** оставьте пустым или `dist`

### Шаг 2: Проверить Build Settings

1. Перейдите в **Settings → Build and Deployment**
2. Проверьте настройки:

#### Build Command (с Override):
```bash
cd ../.. && npm run build --filter=@dialogue-constructor/core
```

Или:
```bash
cd ../.. && npm run build
```

#### Install Command (с Override):
```bash
cd ../.. && npm install
```

#### Output Directory:
Оставьте пустым или `dist`

**⚠️ ВАЖНО:** Убедитесь, что включен **Override** для Build Command и Install Command!

### Шаг 3: Настроить Functions

Vercel должен понимать, что это Node.js приложение:

1. В Vercel: **Settings → Functions**
2. Убедитесь, что указано:
   - **Runtime:** `Node.js 20.x` (или последняя версия)

### Шаг 4: Создать или обновить vercel.json

Создайте или обновите `packages/core/vercel.json`:

```json
{
  "version": 2,
  "builds": [
    {
      "src": "dist/index.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "dist/index.js"
    }
  ],
  "env": {
    "NODE_ENV": "production"
  }
}
```

### Шаг 5: Проверить package.json

Убедитесь, что в `packages/core/package.json` правильно указан `main`:

```json
{
  "main": "./dist/index.js",
  "scripts": {
    "start": "node dist/index.js"
  }
}
```

### Шаг 6: Пересобрать проект

1. В Vercel: **Deployments**
2. Найдите последний деплой
3. Нажмите три точки (⋮) → **Redeploy**
4. Или создайте новый деплой из GitHub

## 🔍 Альтернативное решение: Использовать serverless functions

Если стандартное решение не работает, можно настроить Vercel как serverless function:

### Вариант A: Создать api/health.ts

Создайте файл `packages/core/api/health.ts`:

```typescript
import type { VercelRequest, VercelResponse } from '@vercel/node';

export default async function handler(
  req: VercelRequest,
  res: VercelResponse,
) {
  // Здесь код для health check
  res.json({
    status: 'ok',
    timestamp: new Date().toISOString(),
  });
}
```

Но это не оптимально, так как нужно переделать всю структуру.

## 🎯 Рекомендуемое решение

**Используйте правильные настройки Build Command:**

1. **Settings → Build and Deployment**
2. **Build Command (с Override):**
   ```bash
   cd ../.. && npm run build
   ```
3. **Output Directory:** оставьте пустым
4. **Install Command (с Override):**
   ```bash
   cd ../.. && npm install
   ```

**Важно:** После изменения настроек **обязательно пересоберите проект!**

## 📝 После исправления

Проверьте:
```
https://lego-bot-core.vercel.app/health
```

Должен вернуться JSON:
```json
{
  "status": "ok",
  "databases": {
    "postgres": "connected",
    "redis": "connected"
  }
}
```

## ⚠️ Если все еще не работает

1. Убедитесь, что проект настроен как **Node.js приложение**, а не статический сайт
2. Проверьте, что `dist/index.js` существует после сборки
3. Проверьте логи Vercel на ошибки сборки
4. Попробуйте удалить проект и создать заново с правильными настройками


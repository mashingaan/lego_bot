# Диагностика проблемы с webhook

## Шаг 1: Проверьте webhook info

Откройте в браузере:
```
https://api.telegram.org/bot8585269589:AAGNheAjAdj5p6FJ6Xi-NCZk-fW3g1wYFDQ/getWebhookInfo
```

Проверьте:
- `url` - должен быть `https://lego-bot-core.vercel.app/api/webhook` (или ваш URL)
- `pending_update_count` - сколько необработанных обновлений
- `last_error_message` - если есть ошибка, она будет здесь

## Шаг 2: Проверьте логи Vercel

1. Откройте Vercel Dashboard → проект `lego-bot-core`
2. Перейдите в **Deployments** → последний деплой → **Logs**
3. Отправьте команду `/start` боту
4. Проверьте логи - должны появиться:
   - `🚀 Webhook handler called` - функция вызвана
   - `📨 Webhook request received` - запрос получен
   - `✅ Core module loaded` - модуль загружен
   - `✅ Bot instance found` - бот найден
   - Или ошибки, если что-то не так

## Шаг 3: Проверьте health endpoint

Откройте в браузере:
```
https://lego-bot-core.vercel.app/health
```

Должен вернуться JSON с информацией о состоянии сервиса.

## Шаг 4: Если логи не появляются

Если в логах нет записей `🚀 Webhook handler called`, значит:
- Webhook не доходит до Vercel
- Или функция не вызывается

Проверьте:
1. Правильность URL в webhook (должен совпадать с вашим Vercel URL)
2. Что деплой завершился успешно
3. Что нет ошибок компиляции

## Шаг 5: Если логи есть, но бот не отвечает

Если логи показывают, что запрос приходит, но бот не отвечает:
1. Проверьте, что `botInstance` найден (`✅ Bot instance found`)
2. Проверьте, что нет ошибок при обработке обновления
3. Проверьте переменные окружения (`TELEGRAM_BOT_TOKEN`)


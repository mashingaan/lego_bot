# 🎉 Следующие шаги после успешного деплоя фронтенда

Поздравляем! Фронтенд успешно задеплоен на Vercel. Теперь нужно настроить остальные компоненты.

## ✅ Чек-лист настройки

### 1. 🔧 Обновить API URL во фронтенде

Фронтенд сейчас использует `https://your-api-domain.com` для продакшена. Нужно обновить на реальный URL вашего API.

**Вариант A: Если API работает на localhost/сервере**

Обновите `packages/frontend/script.js`:

```javascript
const API_BASE_URL = window.location.hostname === 'localhost' 
  ? 'http://localhost:3000' 
  : 'https://ваш-api-домен.com'; // ← Обновите это
```

**Вариант B: Если API тоже на Vercel**

1. Задеплойте `packages/core` на Vercel (или другой хостинг)
2. Обновите `API_BASE_URL` на URL деплоя

### 2. 🔐 Обновить FRONTEND_URL в основном боте

В `.env` файле основного бота (`packages/core`) убедитесь, что:

```env
FRONTEND_URL=https://lego-bot-frontend.vercel.app
```

Это нужно для CORS.

### 3. 🔑 Настроить Telegram Login Widget

**Шаг 1: Настроить домен в @BotFather**

1. Откройте Telegram и найдите `@BotFather`
2. Отправьте команду `/setdomain`
3. Выберите вашего бота (например, `@legobusiness1_bot`)
4. Укажите домен: `lego-bot-frontend.vercel.app`

**Шаг 2: Проверить настройки во фронтенде**

В `packages/frontend/index.html` проверьте:

```html
<div id="telegram-login" 
     data-telegram-login="legobusiness1_bot"  <!-- ← Имя вашего бота БЕЗ @ -->
     data-size="large" 
     data-onauth="onTelegramAuth(user)" 
     data-request-access="write">
</div>
```

### 4. 🚀 Запустить основной бот (packages/core)

Основной бот должен быть запущен и доступен по API URL.

**Локально:**
```bash
cd packages/core
npm run dev
```

**Или через Docker:**
```bash
docker-compose up -d postgres redis
cd packages/core
npm run dev
```

**Или задеплоить на хостинг** (Heroku, Railway, Render и т.д.)

### 5. 🧪 Протестировать работу

#### Тест 1: Проверить доступность фронтенда

1. Откройте `https://lego-bot-frontend.vercel.app`
2. Должен появиться виджет входа через Telegram

#### Тест 2: Авторизация

1. Нажмите на виджет "Login with Telegram"
2. Разрешите доступ
3. Должен появиться редактор схем

#### Тест 3: Загрузка ботов

1. После входа должны загрузиться ваши боты
2. Если ботов нет, создайте бота через `/create_bot` в основном боте

#### Тест 4: Редактирование схемы

1. Выберите бота из списка
2. Нажмите "Загрузить схему"
3. Отредактируйте JSON схему
4. Нажмите "Сохранить схему"

#### Тест 5: Проверка работы бота

1. Откройте созданного бота в Telegram
2. Отправьте любое сообщение
3. Бот должен ответить согласно схеме

### 6. 🔍 Проверка ошибок

#### Если фронтенд не загружает ботов:

1. Откройте консоль браузера (F12)
2. Проверьте ошибки в Network tab
3. Убедитесь, что `API_BASE_URL` указан правильно
4. Убедитесь, что основной бот запущен и доступен

#### Если виджет Telegram не работает:

1. Проверьте, что домен настроен в @BotFather
2. Проверьте, что имя бота в `data-telegram-login` указано правильно (БЕЗ `@`)
3. Проверьте, что используете HTTPS (Vercel использует HTTPS по умолчанию)

#### Если CORS ошибки:

1. Проверьте, что `FRONTEND_URL` в `.env` совпадает с URL фронтенда
2. Перезапустите основной бот после изменения `.env`

### 7. 📝 Обновить документацию

После настройки всех компонентов можно:
1. Обновить README.md с актуальными URL
2. Добавить инструкции по деплою API
3. Добавить информацию о доменах

## 🎯 Текущий статус

- ✅ Фронтенд задеплоен на Vercel
- ⏳ API URL нужно обновить во фронтенде
- ⏳ FRONTEND_URL нужно обновить в .env основного бота
- ⏳ Telegram Login Widget нужно настроить в @BotFather
- ⏳ Основной бот должен быть запущен/задеплоен
- ⏳ Нужно протестировать все компоненты

## 📞 Помощь

Если что-то не работает:
1. Проверьте логи основного бота
2. Проверьте консоль браузера (F12)
3. Проверьте Network tab в DevTools
4. Убедитесь, что все URL указаны правильно

